name: flutter_draft
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  bloc: ^8.1.2
  bloc_event_transformers: ^1.0.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  meta: ^1.9.1
  permission_handler: ^11.0.1
  provider: ^6.0.5
  quick_blue:
    git: 
      url: "https://github.com/TalalMash/quick.flutter/"
      path: packages/quick_blue
      ref: master
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
